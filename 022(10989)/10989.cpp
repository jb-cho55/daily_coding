#include <iostream>
#include <vector>
#include <queue>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    long long N;
    cin >> N;

    vector<long long> A(N,0);
    for(int i=0; i<N; i++) cin >> A[i];

    queue<int> tmp(N,0);

    for(int i=0; i<5; i++){
        
    }
}